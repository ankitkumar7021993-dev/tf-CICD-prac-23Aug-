name: Terraform CI/CD

on:
  push:
    branches:
      - '**'

  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:

  terraform:
    name: Terraform CI/CD
    runs-on: ubuntu-latest

    steps:

   